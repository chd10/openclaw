version: '3.8'

services:
  openclaw:
    build: .
    container_name: openclaw
    restart: unless-stopped
    ports:
      - "127.0.0.1:5001:5001"
    volumes:
      - /opt/openclaw/data:/data
      - /opt/openclaw:/opt/openclaw
      - /opt/openclaw/agent:/app
    env_file:
      - .env
